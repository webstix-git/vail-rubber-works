import Employment from "@/views/about/Employment";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.employment;

export default function Page() {
  return <Employment />;
}
