import SafetyMissionVision from "@/views/about/SafetyMissionVision";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.safetyMissionVision;

export default function Page() {
  return <SafetyMissionVision />;
}
