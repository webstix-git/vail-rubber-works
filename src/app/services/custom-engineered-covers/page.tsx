import CustomEngineeredCovers from "@/views/services/CustomEngineeredCovers";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.customEngineeredCovers;

export default function Page() {
  return <CustomEngineeredCovers />;
}
