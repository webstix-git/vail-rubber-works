import OtherServices from "@/views/services/OtherServices";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.otherServices;

export default function Page() {
  return <OtherServices />;
}
