import RollDesignFabrication from "@/views/services/RollDesignFabrication";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.rollDesignFabrication;

export default function Page() {
  return <RollDesignFabrication />;
}
