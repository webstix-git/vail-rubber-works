import MandrelSleeves from "@/views/services/MandrelSleeves";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.mandrelSleeves;

export default function Page() {
  return <MandrelSleeves />;
}
