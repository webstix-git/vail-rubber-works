import RollMaintenance from "@/views/services/RollMaintenance";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.rollMaintenance;

export default function Page() {
  return <RollMaintenance />;
}
