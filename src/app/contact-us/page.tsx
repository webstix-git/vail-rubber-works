import ContactUs from "@/views/about/ContactUs";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.contactUs;

export default function Page() {
  return <ContactUs />;
}
