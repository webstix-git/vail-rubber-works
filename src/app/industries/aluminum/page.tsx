import AluminumIndustry from "@/views/industries/AluminumIndustry";
import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata.aluminum;

export default function Page() {
  return <AluminumIndustry />;
}
